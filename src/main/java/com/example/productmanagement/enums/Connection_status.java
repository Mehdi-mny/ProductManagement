package com.example.productmanagement.enums;

public enum Connection_status {
    Default,
    follow_request_received,
    follow_request_sent,
    blocking,
    followed_by,
    following,
    muting

}
